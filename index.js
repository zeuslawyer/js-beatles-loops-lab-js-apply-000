function theBeatlesPlay(musicians, instruments) {
  var arr =[];
  let c = 0;
  for(c ; c < musicians.length; c++) {
    let str = `${musicicians[c]} plays ${instruments[c]}`;
    arr.push(str);
  }
  return arr;
}